[package]
name = "annotator"
version = "0.1.0"
edition = "2024"

[dependencies]
sdl2         = { version = "0.37", features = ["ttf"] }
v4l          = "0.14"
jpeg-decoder = "0.3"
serde        = { version = "1", features = ["derive"] }
toml         = "0.8"
